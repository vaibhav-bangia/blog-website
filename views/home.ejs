<%- include('partials/header'); -%>
<h1>Home</h1> 
<p><%= startingContent %></p>

<% for(let p of posts ) { %>
    <h1><%=p.title %></h1>
    <p>
        <%=p.content.substring(0,100)%>
        <a href="/posts/<%=p._id%>" >Read More</a>
    </p>
<% } %>
